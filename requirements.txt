# Fijada a la misma version que Streamlit Cloud trae preinstalada, para que lo
# que se prueba en local sea exactamente lo que se sirve. Ojo: fijar una version
# MAS ANTIGUA que la de Cloud rompe el despliegue, porque pip intenta degradarla
# y el instalador falla. Igualar hacia arriba, nunca hacia abajo.
# Para subir de version: cambiarla aqui, recrear .venv, probar y desplegar.
streamlit==1.61.1
requests>=2.31,<3
